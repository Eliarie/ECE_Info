# 跨设备收藏同步

“学前教育前沿”的收藏不再只依赖当前浏览器。

## 使用方法

1. 在任意一台已经有收藏的设备上点击顶部“跨设备同步收藏”。
2. 打开系统复制的同步链接。
3. 把同一链接发到其他设备并打开一次；该设备已有收藏会与同一同步组自动合并。
4. 内容工作台读取链接中的 `bookmark_sync` 值，只同步该组真实收藏，不再读取公共最新文章。

同步链接中的 UUID 相当于单用户收藏同步码，不应公开发布。当前是无账号的轻量同步方案，不是完整登录账户体系。

## 部署要求

在 Supabase SQL Editor 应用：

```text
supabase/migrations/20260810090000_add_cross_device_saved_article_read.sql
```

该迁移只允许按给定同步码读取对应的文章 ID；文章正文元数据仍使用现有公开只读表。网站不会借此修改文章数据。
